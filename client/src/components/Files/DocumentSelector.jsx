import{Check,FileText,X}from"lucide-react";export default function DocumentSelector({open,onClose,documents=[],selected=[],onToggle}){if(!open)return null;return <div className="fixed inset-0 z-50 grid place-items-center bg-black/50 p-4"><div className="w-full max-w-lg rounded-2xl border border-zinc-200 bg-white p-4 shadow-2xl dark:border-zinc-800 dark:bg-zinc-950"><div className="mb-4 flex items-center justify-between"><div><h3 className="font-semibold">Attach knowledge</h3><p className="text-xs text-zinc-500">Select indexed documents for this task.</p></div><button className="icon-btn" onClick={onClose}><X size={17}/></button></div><div className="max-h-80 space-y-2 overflow-auto">{documents.map(d=>{const id=d.id||d._id,active=selected.includes(id);return <button key={id} onClick={()=>onToggle(id)} className={`flex w-full items-center gap-3 rounded-xl border p-3 text-left ${active?"border-violet-500 bg-violet-500/10":"border-zinc-200 dark:border-zinc-800"}`}><FileText size={18}/><div className="min-w-0 flex-1"><div className="truncate text-sm font-medium">{d.filename}</div><div className="text-xs text-zinc-500">{d.chunkCount||0} chunks · {d.status}</div></div>{active&&<Check size={17} className="text-violet-500"/>}</button>})}{!documents.length&&<p className="py-10 text-center text-sm text-zinc-500">Upload documents from the Documents page first.</p>}</div></div></div>}
