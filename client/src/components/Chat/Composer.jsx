import{useEffect,useRef}from"react";import{Paperclip,Send,Square}from"lucide-react";export default function Composer({value,onChange,onSend,running,onStop,selectedCount=0,onOpenFiles}){const ref=useRef(null);useEffect(()=>{if(ref.current){ref.current.style.height="0px";ref.current.style.height=Math.min(ref.current.scrollHeight,180)+"px"}},[value]);return <div className="border-t border-zinc-200 bg-white p-4 dark:border-zinc-800 dark:bg-zinc-950"><div className="mx-auto max-w-4xl rounded-2xl border border-zinc-300 bg-white shadow-sm focus-within:border-violet-500 dark:border-zinc-700 dark:bg-zinc-900"><textarea ref={ref} rows={1} value={value} onChange={e=>onChange(e.target.value)} onKeyDown={e=>{if(e.key==="Enter"&&!e.shiftKey){e.preventDefault();if(!running)onSend()}}} placeholder="Give your AI team a complex task..." className="max-h-[180px] min-h-14 w-full resize-none bg-transparent px-4 pt-4 text-sm outline-none placeholder:text-zinc-500"/><div className="flex items-center justify-between px-3 pb-3"><button onClick={onOpenFiles} className="secondary-btn"><Paperclip size={15}/>Files{selectedCount?` (${selectedCount})`:""}</button>{running?<button onClick={onStop} className="secondary-btn"><Square size={14}/>Stop</button>:<button onClick={onSend} disabled={!value.trim()} className="primary-btn disabled:cursor-not-allowed disabled:opacity-40"><Send size={15}/>Send</button>}</div></div><p className="mx-auto mt-2 max-w-4xl text-center text-[11px] text-zinc-500">Agent outputs can be imperfect. Review important facts and decisions.</p></div>}
